{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ \"$f\" == *.rs || \"$f\" == */Cargo.toml ]] && touch \"$f\" || true; }",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
